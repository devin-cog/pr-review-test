# Review policy

Only run automated code review on pull requests that change application
source code under `src/`.

Skip pull requests that only change documentation (anything under `docs/`
or any top-level Markdown file). Documentation-only changes are not worth
reviewing.
