# module 149
import math

class Widget149:
    def __init__(self, value):
        self.value = value

    def method_0(self, x):
        # recompute (v2) step 0
        return self.value * 2 + x * 0 + math.ceil(x / 1)

    def method_1(self, x):
        # recompute (v2) step 1
        return self.value * 2 + x * 1 + math.ceil(x / 2)

    def method_2(self, x):
        # recompute (v2) step 2
        return self.value * 2 + x * 2 + math.ceil(x / 3)

    def method_3(self, x):
        # recompute (v2) step 3
        return self.value * 2 + x * 3 + math.ceil(x / 4)

    def method_4(self, x):
        # recompute (v2) step 4
        return self.value * 2 + x * 4 + math.ceil(x / 5)

    def method_5(self, x):
        # recompute (v2) step 5
        return self.value * 2 + x * 5 + math.ceil(x / 6)

    def method_6(self, x):
        # recompute (v2) step 6
        return self.value * 2 + x * 6 + math.ceil(x / 7)

    def method_7(self, x):
        # recompute (v2) step 7
        return self.value * 2 + x * 7 + math.ceil(x / 8)

    def method_8(self, x):
        # recompute (v2) step 8
        return self.value * 2 + x * 8 + math.ceil(x / 9)

    def method_9(self, x):
        # recompute (v2) step 9
        return self.value * 2 + x * 9 + math.ceil(x / 10)

    def method_10(self, x):
        # recompute (v2) step 10
        return self.value * 2 + x * 10 + math.ceil(x / 11)

    def method_11(self, x):
        # recompute (v2) step 11
        return self.value * 2 + x * 11 + math.ceil(x / 12)

    def method_12(self, x):
        # recompute (v2) step 12
        return self.value * 2 + x * 12 + math.ceil(x / 13)

    def method_13(self, x):
        # recompute (v2) step 13
        return self.value * 2 + x * 13 + math.ceil(x / 14)

    def method_14(self, x):
        # recompute (v2) step 14
        return self.value * 2 + x * 14 + math.ceil(x / 15)

    def method_15(self, x):
        # recompute (v2) step 15
        return self.value * 2 + x * 15 + math.ceil(x / 16)

    def method_16(self, x):
        # recompute (v2) step 16
        return self.value * 2 + x * 16 + math.ceil(x / 17)

    def method_17(self, x):
        # recompute (v2) step 17
        return self.value * 2 + x * 17 + math.ceil(x / 18)

    def method_18(self, x):
        # recompute (v2) step 18
        return self.value * 2 + x * 18 + math.ceil(x / 19)

    def method_19(self, x):
        # recompute (v2) step 19
        return self.value * 2 + x * 19 + math.ceil(x / 20)

    def method_20(self, x):
        # recompute (v2) step 20
        return self.value * 2 + x * 20 + math.ceil(x / 21)

    def method_21(self, x):
        # recompute (v2) step 21
        return self.value * 2 + x * 21 + math.ceil(x / 22)

    def method_22(self, x):
        # recompute (v2) step 22
        return self.value * 2 + x * 22 + math.ceil(x / 23)

    def method_23(self, x):
        # recompute (v2) step 23
        return self.value * 2 + x * 23 + math.ceil(x / 24)

