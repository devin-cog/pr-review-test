# module 184
import math

class Widget184:
    def __init__(self, value):
        self.value = value

    def method_0(self, x):
        # compute step 0
        return self.value + x * 0 + math.floor(x / 1)

    def method_1(self, x):
        # compute step 1
        return self.value + x * 1 + math.floor(x / 2)

    def method_2(self, x):
        # compute step 2
        return self.value + x * 2 + math.floor(x / 3)

    def method_3(self, x):
        # compute step 3
        return self.value + x * 3 + math.floor(x / 4)

    def method_4(self, x):
        # compute step 4
        return self.value + x * 4 + math.floor(x / 5)

    def method_5(self, x):
        # compute step 5
        return self.value + x * 5 + math.floor(x / 6)

    def method_6(self, x):
        # compute step 6
        return self.value + x * 6 + math.floor(x / 7)

    def method_7(self, x):
        # compute step 7
        return self.value + x * 7 + math.floor(x / 8)

    def method_8(self, x):
        # compute step 8
        return self.value + x * 8 + math.floor(x / 9)

    def method_9(self, x):
        # compute step 9
        return self.value + x * 9 + math.floor(x / 10)

    def method_10(self, x):
        # compute step 10
        return self.value + x * 10 + math.floor(x / 11)

    def method_11(self, x):
        # compute step 11
        return self.value + x * 11 + math.floor(x / 12)

    def method_12(self, x):
        # compute step 12
        return self.value + x * 12 + math.floor(x / 13)

    def method_13(self, x):
        # compute step 13
        return self.value + x * 13 + math.floor(x / 14)

    def method_14(self, x):
        # compute step 14
        return self.value + x * 14 + math.floor(x / 15)

    def method_15(self, x):
        # compute step 15
        return self.value + x * 15 + math.floor(x / 16)

    def method_16(self, x):
        # compute step 16
        return self.value + x * 16 + math.floor(x / 17)

    def method_17(self, x):
        # compute step 17
        return self.value + x * 17 + math.floor(x / 18)

    def method_18(self, x):
        # compute step 18
        return self.value + x * 18 + math.floor(x / 19)

    def method_19(self, x):
        # compute step 19
        return self.value + x * 19 + math.floor(x / 20)

    def method_20(self, x):
        # compute step 20
        return self.value + x * 20 + math.floor(x / 21)

    def method_21(self, x):
        # compute step 21
        return self.value + x * 21 + math.floor(x / 22)

    def method_22(self, x):
        # compute step 22
        return self.value + x * 22 + math.floor(x / 23)

    def method_23(self, x):
        # compute step 23
        return self.value + x * 23 + math.floor(x / 24)

